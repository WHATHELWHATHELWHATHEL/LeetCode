/**
 * @param {number} n
 * @return {boolean}
 */

const canWinNim = (n) => { // eslint-disable-lines
  // take all the remain, win
  if (n === 1 || n === 2 || n === 3) {
    return true;
  }
  // 
};
